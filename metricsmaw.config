{port,18000}.

% set up reporters that can be configured to do something with the metrics.
{reporters,
   [{
      csv_reporter,[
         {enabled,true}]
     },
     {
       console_reporter,[
          {enabled,true}
       ]
     }
   ]
}.